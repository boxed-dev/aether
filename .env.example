# ===========================================
# AETHER LINK - Environment Variables
# ===========================================
# Copy this file to .env and update values

# Database (PostgreSQL)
DATABASE_URL=postgresql://aether:aether_dev_password@localhost:5433/aether_link

# ===========================================
# apps/api/.env
# ===========================================
# DATABASE_URL=postgresql://aether:aether_dev_password@localhost:5433/aether_link
# AUTH_SECRET=<run: openssl rand -base64 32> (MUST match dashboard AUTH_SECRET)
#
# CORS Configuration:
# ALLOWED_ORIGINS - Comma-separated list of allowed origins for production
# Examples:
#   ALLOWED_ORIGINS=https://dashboard.example.com,https://www.example.com
#
# Note: The API automatically allows:
#   - Localhost URLs (http://localhost:*) in local development
#   - Vercel preview URLs (https://*.vercel.app) when VERCEL_ENV=preview or development
#   - Origins specified in ALLOWED_ORIGINS (production custom domains)
#
# Vercel Deployment:
# - Set ALLOWED_ORIGINS in production environment to your custom domains
# - Preview deployments automatically work with *.vercel.app URLs
# - No need to manually configure origins for preview branches
#
# Rate Limiting (automatic, no configuration needed):
# - Auth endpoints: 5 requests/minute per IP
# - Write operations (POST/PATCH/DELETE): 30 requests/minute per IP
# - Read operations (GET): 100 requests/minute per IP
# - Click tracking: 60 requests/minute per IP

# ===========================================
# apps/dashboard/.env.local
# ===========================================
# NEXT_PUBLIC_API_URL=http://localhost:3001
# NEXT_PUBLIC_RENDERER_URL=http://localhost:3002
# AUTH_SECRET=<run: openssl rand -base64 32> (MUST match API AUTH_SECRET)
#
# Vercel Deployment Instructions:
#
# Local Development:
#   NEXT_PUBLIC_API_URL=http://localhost:3001
#   NEXT_PUBLIC_RENDERER_URL=http://localhost:3002
#
# Vercel Preview (set in Project Settings > Environment Variables > Preview):
#   NEXT_PUBLIC_API_URL=https://your-api-project-hash.vercel.app
#   NEXT_PUBLIC_RENDERER_URL=https://your-renderer-project-hash.vercel.app
#
# Vercel Production (set in Project Settings > Environment Variables > Production):
#   NEXT_PUBLIC_API_URL=https://api.yourdomain.com
#   NEXT_PUBLIC_RENDERER_URL=https://yourdomain.com
#
# Note: Vercel automatically sets VERCEL_ENV (production/preview/development) and VERCEL_URL

# ===========================================
# apps/renderer/.env.local
# ===========================================
# NEXT_PUBLIC_API_URL=http://localhost:3001
# NEXT_PUBLIC_RENDERER_URL=http://localhost:3002
#
# Vercel Deployment Instructions:
#
# Local Development:
#   NEXT_PUBLIC_API_URL=http://localhost:3001
#   NEXT_PUBLIC_RENDERER_URL=http://localhost:3002
#
# Vercel Preview (set in Project Settings > Environment Variables > Preview):
#   NEXT_PUBLIC_API_URL=https://your-api-project-hash.vercel.app
#   NEXT_PUBLIC_RENDERER_URL=https://your-renderer-project-hash.vercel.app
#
# Vercel Production (set in Project Settings > Environment Variables > Production):
#   NEXT_PUBLIC_API_URL=https://api.yourdomain.com
#   NEXT_PUBLIC_RENDERER_URL=https://yourdomain.com
#
# Note: Vercel automatically sets VERCEL_ENV (production/preview/development) and VERCEL_URL
