# ===========================================
# VERCEL DEPLOYMENT - Environment Variables
# ===========================================
# Copy the relevant section to each Vercel project's environment variables

# ===========================================
# PROJECT 1: API (@aether-link/api)
# Root Directory: apps/api
# ===========================================
DATABASE_URL=postgresql://user:password@host:port/database
NODE_ENV=production
# Optional: Only needed if using custom domains
ALLOWED_ORIGINS=https://yourdomain.com,https://dashboard.yourdomain.com

# ===========================================
# PROJECT 2: DASHBOARD (@aether-link/dashboard)
# Root Directory: apps/dashboard
# ===========================================
NEXT_PUBLIC_API_URL=https://your-api-project.vercel.app
NEXT_PUBLIC_RENDERER_URL=https://your-renderer-project.vercel.app
AUTH_SECRET=<generate-with-openssl-rand-base64-32>
NODE_ENV=production

# ===========================================
# PROJECT 3: RENDERER (@aether-link/renderer)
# Root Directory: apps/renderer
# ===========================================
NEXT_PUBLIC_API_URL=https://your-api-project.vercel.app
NEXT_PUBLIC_RENDERER_URL=https://your-renderer-project.vercel.app
NODE_ENV=production

# ===========================================
# GENERATE AUTH_SECRET
# ===========================================
# Run this command to generate a secure AUTH_SECRET:
# openssl rand -base64 32
#
# Use the SAME value in both API and Dashboard projects

# ===========================================
# DATABASE OPTIONS
# ===========================================
# Vercel Postgres:
#   - Format: postgres://user:pass@host:5432/verceldb?sslmode=require
#   - Get from Vercel Dashboard → Storage → Your Database
#
# Supabase:
#   - Format: postgresql://postgres:password@host.supabase.co:5432/postgres
#   - Get from Project Settings → Database
#
# Railway:
#   - Format: postgresql://postgres:password@containers.railway.app:5432/railway
#   - Get from Database service → Connect
#
# Neon:
#   - Format: postgresql://user:password@host.neon.tech:5432/neondb?sslmode=require
#   - Get from Dashboard → Connection Details

# ===========================================
# VERCEL PROJECT SETTINGS
# ===========================================
# For each project, configure in Vercel Dashboard:
#
# 1. Framework Preset: Next.js
# 2. Root Directory: apps/api (or apps/dashboard or apps/renderer)
# 3. Build Command: (leave empty - uses vercel.json)
# 4. Install Command: (leave empty - uses vercel.json)
# 5. Output Directory: .next
# 6. Node.js Version: 20.x (automatic)
